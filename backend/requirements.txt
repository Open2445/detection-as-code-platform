# Web framework
fastapi==0.109.2
uvicorn[standard]==0.27.1

# Database
sqlalchemy==2.0.27
psycopg2-binary==2.9.9
alembic==1.13.1

# Validation / settings
pydantic==2.6.1
pydantic-settings==2.2.0

# File uploads
python-multipart==0.0.9

# YAML parsing (for Sigma rules)
pyyaml==6.0.1

# Date/time utilities
python-dateutil==2.9.0

# CSV export
# (stdlib csv module used — no additional dependency)

# HTTP client (for tests)
httpx==0.27.0

# Testing
pytest==8.0.2
pytest-asyncio==0.23.5

# pySigma (abstraction layer reference — evaluator is custom)
# sigma-cli is available as a future CLI backend option
# pip install sigma-cli  # optional, not required for v1
